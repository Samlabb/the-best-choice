server:
  port: 8082

management:
  server:
    port: 8082

spring:
  application:
    name: voting
  datasource:
    url: jdbc:postgresql://localhost:5432/movie_choice_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: schema_voting

tmdb:
  api-key: ${TMDB_API_KEY:a55a61a6c703616203db28065029d325}
  base-url: ${TMDB_BASE_URL:https://api.themoviedb.org/3}
  image-base-url: ${TMDB_IMAGE_BASE_URL:https://image.tmdb.org/t/p/w500}